FROM python:3.11-slim

WORKDIR /app
COPY api/api_server.py .
RUN pip install fastapi uvicorn httpx "python-multipart" "aiofiles" "fastapi[all]"

CMD ["uvicorn", "api_server:app", "--host", "0.0.0.0", "--port", "8000"]
